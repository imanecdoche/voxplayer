package com.vox.music.ui.theme

import androidx.compose.ui.graphics.Color

// Pure Monochrome Palette tokens
val PureBlack = Color(0xFF000000)
val PureWhite = Color(0xFFFFFFFF)
val NeutralGray = Color(0xFF757575)

// Divider tokens (0.5.dp - 1.dp hairline)
val DarkDivider = Color(0xFF222222)
val LightDivider = Color(0xFFE0E0E0)

// Surface tokens (Zero-elevation / flat)
val DarkSurface = Color(0xFF000000)
val LightSurface = Color(0xFFFFFFFF)

// Accent / Playhead / Marker tokens
val DarkMarkerAB = Color(0xFFFFFFFF)
val LightMarkerAB = Color(0xFF000000)
