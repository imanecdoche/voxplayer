# Vox — Workspace Rules & Project Instructions

Dokumen ini merupakan acuan utama dan mutlak untuk proyek **Vox (Minimalist Offline Music Player & Audio Utility)**.

## 📌 Absolute Rules & Directives
1. **Aturan Mutlak**: Setiap ada instruksi tambahan baru, langsung catat ke `.agents/RULES.md` dan jadikan file instruksi ini sebagai acuan utama dan mutlak selalu benar.
2. **Platform & Stack**:
   - Platform: Android Native (Kotlin 2.0+, Kotlin DSL `build.gradle.kts`).
   - Target SDK: Android 10 (API 29) s/d Android 15 (API 35).
   - UI: Jetpack Compose + Material 3 (Monokrom murni).
   - Audio Core: Jetpack Media3 (ExoPlayer + MediaSessionService).
   - Database: AndroidX Room.
   - DI: Dagger Hilt.
3. **Design System & Visual Assets (Ultra-Minimalist Monochrome)**:
   - **Zero Container / Zero Card Elevation**: Dilarang menggunakan card elevated, drop shadows, atau container berlapis.
   - **Pure Monochrome**: Hanya menggunakan `#000000` (Amoled Black), `#FFFFFF` (White), `#757575` (Neutral Gray), `#222222` (Dark Divider), `#E0E0E0` (Light Divider).
   - **Typography-Driven & Whitespace Layout**: Hirarki dibangun menggunakan bobot font (`FontWeight`), `Spacer`, dan hairline dividers (`0.5.dp` - `1.dp`).
   - **Sharp Corners**: Cover art 1:1 tajam tanpa corner radius melengkung.
   - **Branding Assets**:
     - App Icon: `ic_launcher` & `ic_launcher_round` (`res/mipmap-*`).
     - Vector Logomark: `res/drawable/vox_main_logomark.xml` (`VoxLogomark` composable).
     - Vector Logotype: `res/drawable/vox_main_logotype.xml` (`VoxLogotype` composable).
     - Brand Header: `VoxHeader` composable monokrom flat tanpa container.
4. **Tahap Pengembangan**:
   - **Phase 1 Step 1**: Inisialisasi Proyek, Gradle Kotlin DSL, Version Catalog, Manifest Permissions & Foreground Service, Pure Monochrome Design Tokens. (DONE)
   - **Phase 1 Step 2**: Visual Assets Integration, Room Database Entity/DAO/Database, MediaStore Audio Scanner & ContentObserver, Clean Architecture Repository, Library Screen UI (Folder & Track Views) with MVI, Runtime Permissions Handling. (DONE)
   - **Phase 1 Step 3**: Jetpack Media3 (ExoPlayer + MediaSessionService), SonicAudioProcessor DSP Pipeline, Precision A-B Looping, PlayerController StateFlow, PlayerScreen & MiniPlayer UI. (DONE)
   - **Phase 2 Step 4**: SonicAudioProcessor Independent Speed (0.25x-3.00x) & Pitch Shifter (-12 s.t to +12 s.t) Engine, State/ViewModel Reducer, Precision Step Controls, Preset Chips, Flat Monochrome DSP Controller Sheet. (DONE)
   - **Phase 2 Step 5**: Multi-band Graphic Equalizer (Android AudioEffect), BassBoost, Virtualizer, LoudnessEnhancer, Preset Engine, Precision A-B Looper Visual Marker, EqualizerScreen Flat Monochrome. (DONE)
   - **Phase 2 Step 6**: Custom Playlists (Create/Add/Reorder/Delete), M3U/M3U8 Export-Import, Local Custom Tags, Scoped Storage File Operations (Rename & System Delete Request API 30+), Track Actions BottomSheet. (DONE)
   - **Phase 3 Step 7**: Metadata Inspector & ID3 Tag Editor (Jaudiotagger Engine, Title/Artist/Album/Year/Genre/Lyrics/Artwork binary read-write, Scoped Storage Write Request API 30+, MetadataEditorScreen & AudioInspectorBottomSheet). (DONE)
   - **Phase 3 Step 8**: Audio Clipper & Trimmer Engine (Visual Waveform Trimmer, Fast Lossless Trimming via MediaExtractor/MediaMuxer, Custom Bitrate Exporter, AudioClipperScreen Flat Monochrome). (DONE)
   - **Phase 3 Step 9**: Synced Lyrics Viewer & LRC Parser Engine (.lrc external parser, embedded USLT/SYLT extractor, real-time index synchronization, seek-by-lyric, smooth auto-scrolling, LyricsScreen Flat Monochrome). (DONE)
   - **Phase 4 Step 10**: NDK C++ Integration & Native Audio Signal Processing Bridge (CMake, libvox-dsp.so, AMediaExtractor/AMediaCodec PCM decoding, JNI bridge, Threading & Memory safety). (DONE)
   - **Phase 4 Step 11**: BPM / Tempo & Musical Key Detection Signal Processing Engine (C++ Autocorrelation Onset Detection, Chromagram Harmonic Profiles, AudioAnalysisRepository caching, UI badges & inspector integration). (DONE)
   - **Phase 4 Step 12**: Chord Progression Analyzer Engine (HPCP Windowed Chromagram, Real-time Chord Tracker, ChordVisualizer Flat Monochrome, Room DB Cache). (DONE)
   - **Phase 4 Step 13**: Final UI Polish, Memory & Battery Benchmark, Bug Fixing, and Build Production APK (`Vox_stable_v1.0.0.apk`). (DONE)
   - **Extension Step 14**: Lucide Icons for Compose integration (`com.composables:icons-lucide-android:2.2.1` / `com.composables:icons-lucide:2.2.1`), full icon set refactor (Player, Scrubber, Looper, Library, Equalizer, Clipper, Tag Editor, Lyrics, Analysis, Actions) with pure monochrome styling. (DONE)
   - **Instruction / Remote Sync**: Remote Git repository `git@github.com:imanecdoche/voxplayer.git` configured with root APK (`Vox_stable_v1.0.0.apk`) tracking and staging. (DONE)
   - **Instruction / README & Release**: Comprehensive README.md rewrite (Deskripsi app, spesifikasi teknis, fitur, teknologi, library, arsitektur/flow), Git staging & commit, serta publikasi GitHub Release Notes v1.0.0. (DONE)
   - **Instruction / Cover Art & Rounded UI**: (1) Fix Cover Art extraction & rendering via MediaMetadataRetriever / MediaStore content URI / Coil async caching with minimal monochrome placeholder (Lucide.Music). (2) Apply rounded corners across UI: 12.dp for PlayerScreen cover art, 6.dp for list thumbnails, CircleShape/8.dp for play buttons, 10.dp for Chord Tracker box, 4.dp for BPM/Key badges, 16.dp for all dialogs, bottom sheets, and popups. Rebuild APK to Vox_stable_v1.0.0.apk. (DONE)
   - **Instruction / Floating Capsule Mini Player**: Redesign Mini Player as a floating capsule: CircleShape/RoundedCornerShape(50), dark contrast background (#181818/black), floating margins (horizontal 16.dp, bottom 10.dp + navigationBarsPadding), circular cover art (CircleShape 46dp), centered Title & Artist, compact controls row on the right (SkipBack, Play/Pause with white pill background, SkipForward) with isolated click handling, bottom content padding for list scrolling, and open PlayerScreen on capsule tap. (DONE)
   - **Instruction / PlayerScreen Layout Revamp**: Total redesign of PlayerScreen to match visual mockup: (1) Top Bar: Left ChevronDown, Right Settings, no center title. (2) Center Art Cover: Centered 1:1, width ~70-75%, RoundedCornerShape(28.dp), placeholder fallback. (3) Track Metadata: Center-aligned Title (Bold, 20-22sp) & Artist (757575, 14-15sp). (4) Action Toolbar Row: SpaceBetween (padding 32.dp) with Media mode (SquarePlay/Tv), Favorite (Heart), Add to Playlist (Plus). (5) Scrubber: Minimal slider with elapsed time (left) and total duration (right). (6) Main Controls: 5 evenly-spaced outline monochrome icons (Shuffle, SkipBack, Play/Pause 32dp, SkipForward, Repeat/Repeat1) with no container cards. (DONE)
   - **Instruction / DSP Speed Pitch & A-B Loop Fix**: (1) Integrasikan `SonicAudioProcessor` ke dalam ExoPlayer via custom `DefaultAudioSink.Builder` & `DefaultRenderersFactory`. Sambungkan perubahan Speed (`playbackSpeed`) dan Pitch Shifter Semitones (`pitchFactor = 2.0.pow(semitones / 12.0).toFloat()`) secara real-time ke `sonicAudioProcessor` dan `PlaybackParameters`. (2) Perbaiki logika A-B Looper presisi dengan continuous polling loop (50-100ms) di playback service/controller: jika `currentPos >= pointB || currentPos < pointA`, seek kembali ke `pointA`. (3) Sinkronkan state dua arah dan compile/build release APK `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / Fast-Forward & Skip Forward Fix**: (1) Periksa dan perbaiki handler Forward (Fast-Forward / Seek Forward / Skip Forward) di `MusicPlayerController` dan `PlayerViewModel`: pastikan `seekForward` / `skipNext` menghitung target position dengan aman terhadap total durasi tanpa terhambat oleh `C.TIME_UNSET` (-1) atau durasi 0, serta panggil `controller.seekTo(targetPos)` / `seekToNextMediaItem()`. (2) Pastikan binding tombol Forward di `PlayerScreen`, `MiniPlayerBar`, dan `SpeedPitchSheet` terhubung presisi ke aksi forward yang tepat. (3) Compile debug & release APK ke `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / PlayerScreen Action Toolbar & Top Bar Revamp**: (1) Action Toolbar Tengah: Tombol Kiri (Current Queue Playlist: `Lucide.ListMusic` buka Queue Sheet dengan reorder, hapus track, add track), Tombol Tengah (Favorite: `Lucide.Heart`, outline jika false, merah pastel `#FF6B6B` jika true), Tombol Kanan (Add to Saved Playlist: `Lucide.Plus`, buka dialog AddToPlaylist). (2) Top Bar: Kiri `Lucide.ChevronDown`, Kanan 3 tombol berderet rapi (Lyric: `Lucide.Mic`, Track Info: `Lucide.Info` buka Metadata & Audio Analysis Sheet dengan Key, BPM, Chord timeline, Config/DSP: `Lucide.Settings` buka DSP & Equalizer Sheet). (3) RoundedCornerShape(16.dp) untuk seluruh sheet/dialog, verifikasi dan build `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / Folder Sorting & Search View**: (1) Top Bar Folder View: Tambahkan tombol Search (`Lucide.Search`) dan Sort (`Lucide.ArrowUpDown`). (2) Fitur Sorting: Opsi A to Z, Z to A, Date Added (Terbaru/Terlama), Length/Duration (Terpanjang/Terpendek) dengan persistensi preferensi. (3) Search Screen: Live multi-field filtering (Title, File Name, Artist, Album), Search History di Room DB (`SearchHistoryEntity` & DAO) dengan clear/delete, click-to-query, dan track list results. (4) Verifikasi compile & build `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / Custom UI, Bugfixes, Gestures & Advanced Features**: (1) Custom Seekbar 2dp canvas/line dengan solid circular thumb. (2) Refactor controls (Forward -> Next Track; Backward -> Prev if <=3s, Seek 0 if >3s). (3) Bug Fixes (Favorite responsiveness & instant sync, Favorite list icon filled #FF6B6B pastel red, Search/Sort header onClick reliability, Ghost Hitbox cleanup). (4) Fitur Baru (Queue Add Track Search & Sort, Popup Add to Queue, Queue Reordering, Active Track Animated 3-Bar Waveform Overlay, Vertical Swipe-Down dismiss PlayerScreen, Settings & Dynamic Background via AndroidX Palette API, Horizontal Swipe Next/Prev Track, Sleep Timer 15m/30m/45m/60m/End-of-song/Custom in DSP sheet). (5) Build release APK ke `Vox_stable_v1.0.0.apk`. PENTING: Jangan commit/push ke GitHub. (DONE)
   - **Instruction / ADB Install**: Install dan pasang APK `Vox_stable_v1.0.0.apk` ke perangkat HP via ADB (`adb install -r Vox_stable_v1.0.0.apk`). (DONE)
   - **Instruction / Dynamic BG White, Touch Passthrough, Folder Queue, Clear Queue & Paging Carousel**: (1) PlayerScreen: pasang solid `Color.White` background, touch passthrough blocker modifier, dynamic background aura warna lembut (alpha 15-25%) tanpa vignette hitam pekat, fallback pure white jika tanpa cover art. (2) Fix Click Event Search & Sort di Folder Header. (3) Mini Player: hilangkan lingkaran container di tombol Play/Pause (jadikan polos putih seirama Next/Prev). (4) Folder Default Queue: klik lagu di folder memasukkan seluruh isi folder ke playing queue. (5) Clear Queue: tombol pembersih antrean (Lucide.Eraser/Broom) menghapus semua lagu kecuali lagu aktif saat ini. (6) Gesture Separation: Vertical Drag to dismiss seluruh screen & Horizontal Paging Carousel untuk artwork transition. (7) Build release APK ke `Vox_stable_v1.0.0.apk` dan instal via ADB. (DONE)
   - **Instruction / Lockscreen Lite Player & Preferences**: (1) Settings & Preferences Entry: tambah menu toggle "Kontrol Musik di Layar Kunci" (Lockscreen Player) dengan persistensi SharedPreferences/DataStore (default: true). (2) Lockscreen Lite Activity (`LockscreenPlayerActivity.kt`): daftarkan di AndroidManifest dengan showWhenLocked, turnScreenOn, excludeFromRecents, dan konfigurasi window flags/API 27+. (3) UI Lockscreen Lite Player (Jetpack Compose): artwork rounded 16dp, center title/artist, custom seekbar tipis dengan counter durasi, 5 tombol playback monokrom, tombol dismiss/swipe-up. (4) Service & Broadcast Receiver: pantau `ACTION_SCREEN_ON`, buka `LockscreenPlayerActivity` jika toggle aktif, `isPlaying == true`, dan `KeyguardManager.isKeyguardLocked`. (5) Build APK `Vox_stable_v1.0.0.apk` dan instal via ADB. (DONE)
   - **Instruction / Carousel Single-Artwork Snapping Fix**: (1) Hilangkan `contentPadding` horizontal pada HorizontalPager di `PlayerScreen.kt` agar saat idle HANYA 1 artwork lagu aktif yang terlihat di tengah (100% viewport pager) tanpa ada artwork tetangga yang mengintip di sisi kiri/kanan. (2) Pastikan transisi swipe ke lagu selanjutnya/sebelumnya mulus dengan snapping presisi ke posisi tengah. (3) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / Full Screen Settings, Audio Crossfade & Dynamic BG Intensity**: (1) Full Screen Settings Navigation: buat `SettingsScreen.kt` layar penuh dengan TopAppBar monokrom (`Lucide.ArrowLeft`, judul "SETTINGS & PREFERENCES"), navigasi dari LibraryScreen, dan kategori terstruktur (Playback, Appearance, Lockscreen). (2) Audio Crossfade: toggle switch, VoxSlider durasi (0.5s - 3.0s, step 0.5s, label aktif), dan logika volume fading saat pergantian lagu. (3) Dynamic Background Intensity: VoxSlider 10% - 100% di bawah toggle dynamic background untuk mengatur alpha/opacity gradasi artwork di PlayerScreen. (4) Build APK `Vox_stable_v1.0.0.apk` dan instal via ADB. PENTING: JANGAN commit atau push ke GitHub. (DONE)
   - **Instruction / One UI 7 MediaSession & Notification Optimization**: (1) MediaNotificationProvider & MediaSession Custom Actions: daftarkan tombol Shuffle & Repeat pada system media notification / One UI 7 Live Bar dengan urutan [Shuffle, Previous, Play/Pause, Next, Repeat] dan compact action indices. (2) Branding: set small icon `setSmallIcon(R.drawable.vox_main_logomark)` dan label aplikasi "Vox Player" di AndroidManifest / strings.xml. (3) Build release APK ke `Vox_stable_v1.0.0.apk`. PENTING: JANGAN commit atau push ke GitHub. (DONE)
   - **Instruction / Responsive Glance Home Screen Widget (One UI Squircle)**: (1) Jetpack Glance `SizeMode.Responsive` dengan 3 breakpoint: Compact Square (~120x120dp), Horizontal Bar (~250x100dp), Expanded Large (~250x200dp+). (2) Tiga Mode UI: (A) Compact Square: dark background + low opacity artwork overlay (20-30% alpha) + centered playback controls. (B) Horizontal Bar: dark squircle 24dp, left 1:1 artwork 12dp rounded, right metadata + full controls (Shuffle, Prev, Play/Pause, Next, Repeat). (C) Expanded Large: top full track controls + bottom "Current Queue / Up Next" list (2-3 tracks). (3) Interaksi & Real-time Sync: click track di queue memutar lagu, click background membuka app, update state reaktif dari Service. (4) Build APK `Vox_stable_v1.0.0.apk`. PENTING: JANGAN commit atau push ke GitHub. (DONE)
   - **Instruction / Advanced Customization Activity & Dynamic Theme Engine (`adcuzActivity.kt`)**: (1) Data Model & Serialization: `VoxThemeConfig`, `ColorSchemeConfig`, `DimensionsConfig`, `OffsetsConfig`, `PaddingsConfig`, `AssetPathsConfig`. (2) Persistence & Theme File Manager: Internal dir `theme_engine/`, image ingestion (WEBP 85% max 1080x2400), icon replacement & `.voxpack`/`.zip` importer, export/import `.voxtheme` bundle. (3) System-Wide Composable Injection: `LocalVoxTheme` composition local, runtime dynamic theme state, background layers, custom icons & tokens. (4) UI `adcuzActivity.kt`: Sticky 35% `LiveThemePreviewBox` (Capsule, Seekbar, Controls preview) & 5 Tabs (Colors, Dimensions & Positions, Assets & Backgrounds, Icons, Theme Manager). (5) Build release APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / ADB Install (Theme Engine & Advanced Customization)**: Instal dan pasang APK `Vox_stable_v1.0.0.apk` ke perangkat HP via ADB (`adb install -r Vox_stable_v1.0.0.apk`). (DONE)
   - **Bugfix / MediaSession CommandButton Crash Fix**: Memperbaiki `IllegalArgumentException: sessionCommand is already set. Only one of sessionCommand and playerCommand should be set` pada `MusicPlaybackService.kt` dan `VoxMediaNotificationProvider.kt` dengan menghapus deklarasi ganda `setPlayerCommand` pada custom session buttons (Shuffle & Repeat). Rebuild dan pasang ulang APK via ADB. (DONE)
   - **Instruction / Hold and Drag to Reorder Playing Queue**: (1) Gestur & State: Long-press (hold) detection dengan `detectDragGesturesAfterLongPress`, haptic feedback getaran halus `HapticFeedbackType.LongPress` saat item terangkat. (2) Visual Effects: `zIndex = 1f`, scale `1.03f` dengan animasi spring, Y-axis translation real-time. (3) Animasi Bergeser Item Lain: `Modifier.animateItem()` pada LazyColumn items, instant list swapping saat drag melintasi batas tinggi tetangga. (4) Sinkronisasi Playback: `onDragEnd` commit posisi ke ExoPlayer/MediaSession timeline tanpa menghentikan pemutaran lagu aktif. (5) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / Dynamic Bluetooth Output Manager & Audio Routing**: (1) Deteksi Bluetooth & Dynamic Top Bar Icon: Pantau status audio route via `AudioDeviceCallback` / `AudioManager`, tampilkan icon Bluetooth di Top Bar `PlayerScreen` hanya saat audio aktif tersambung ke perangkat Bluetooth (A2DP / Headset). (2) BottomSheet "AUDIO OUTPUT & EFFECTS" (Rounded 20.dp monokrom): Nama device & status badge, Audio Output Switcher (Bluetooth vs Phone Speaker via `setPreferredDevice` / `setCommunicationDevice`), Toggle Dolby Atmos / Spatial Sound (Spatializer API Android 13+ / SoundAlive detection). (3) Kompilasi dan build release APK `Vox_stable_v1.0.0.apk`. (DONE)
   - **Instruction / Git Stage, Commit & Push**: Melakukan staging seluruh perubahan fitur terbaru (Dynamic Theme Engine, Advanced Customization, Glance Widget, Hold & Drag Reorder Queue, Bluetooth Audio Route Manager), membuat git commit, dan melakukan push ke remote repository GitHub `origin/main`. (DONE)
   - **Instruction / ADB Install & Launch**: Instal APK `Vox_stable_v1.0.0.apk` ke perangkat HP via ADB (`adb install -r Vox_stable_v1.0.0.apk`) dan langsung buka aplikasi dengan `am start -n com.vox.music/.MainActivity`. (DONE)
   - **Instruction / Alphabet Fast Scroll Indexer Bar on Tracklist Folder**: (1) Visibility Guard: Tampilkan bar alfabet di sisi kanan hanya saat mode sorting A-Z (A..Z, #) atau Z-A (Z..A, #), sembunyikan jika Date Added/Duration. (2) UI & Gesture: Column vertikal tipis di `Alignment.CenterEnd`, teks abu-abu (#757575), gestur `pointerInput` (drag + tap) real-time. (3) Floating Alphabet Indicator Popup: Kotak/squircle monokrom (#1E1E1E dengan border putih tipis) di tengah/samping dengan huruf kapital besar dan animasi fade in/out. (4) Scroll Fast-Jump: `lazyListState.scrollToItem(firstIndex)` berdasarkan karakter huruf awal track, haptic feedback `HapticFeedbackType.TextHandleMove` saat berpindah huruf. (5) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / Dynamic Motion Blur in Settings & UI Transitions**: (1) Settings & Preferences: Toggle switch "Motion Blur" (default: false), Custom thin 2dp VoxSlider Intensitas (1 - 10 / 1dp - 25dp blur radius, hanya muncul jika aktif), persistensi `vox_prefs` (`motion_blur_enabled`, `motion_blur_intensity`). (2) Dynamic RenderEffect Motion Blur Modifier: `Modifier.dynamicMotionBlur(velocity, intensity, isEnabled)` berbasis `RenderEffect.createBlurEffect` (API 31+ Android 12-15) dengan safe fallback API < 31. (3) Integrasi Target UI: Artwork Horizontal Carousel Swipe (blur terarah proporsional kecepatan geser, auto-sharp saat idle) & Bottom Sheet / Panel Transition. (4) Kompilasi dan build release APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / Queue Drag Bugfix & Edge Auto-Scroll System**: (1) Touch Slop & Scroll Disabling: Nonaktifkan scroll LazyColumn saat drag aktif (`userScrollEnabled = draggingIndex == null`), konsumsi touch event secara eksklusif agar pointer gesture tidak lepas/drop saat jari bergerak cepat. (2) Edge Auto-Scroll Window: Pasang zona threshold atas & bawah (~60dp); jika jari/item berada di zona atas/bawah, picu continuous coroutine auto-scroll (`scrollBy(-delta)` / `scrollBy(delta)`) dengan percepatan dinamis berbasis jarak ke tepi layar; hentikan otomatis saat jari kembali ke tengah atau drag selesai. (3) Real-time Reorder & Viewport Sync: Sinkronisasi realtime kalkulasi list swap saat auto-scroll berlangsung dan commit ke ExoPlayer saat drag end. (4) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / ADB Install & Launch (Motion Blur & Edge Auto-Scroll)**: Build release APK `Vox_stable_v1.0.0.apk`, instal ke HP via ADB (`adb install -r Vox_stable_v1.0.0.apk`), dan langsung buka aplikasi dengan `am start -n com.vox.music/.MainActivity`. (DONE)
   - **Instruction / Bluetooth Battery Level Indicator in Audio Output Panel**: (1) Izin & Manifest: Pastikan `BLUETOOTH_CONNECT` dideklarasikan dan dihandle aman. (2) Bluetooth Battery Level Receiver: Action `android.bluetooth.device.action.BATTERY_LEVEL_CHANGED`, ekstraksi `android.bluetooth.device.extra.BATTERY_LEVEL` (0-100% atau -1 jika tidak didukung), serta inisialisasi awal via `BluetoothDevice.getBatteryLevel()` (reflection / API 31). (3) UI Audio Output Bottom Sheet: Tampilkan icon baterai monokrom dan persentase di samping nama perangkat yang terhubung hanya jika level valid (>= 0). (4) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / Animation Curve (Easing) & Duration Customizer**: (1) Data Store & Model: Enum `VoxEasingCurve` (LINEAR, EASE_IN, EASE_OUT, EASE_IN_OUT, SINE_QUAD, CUBIC_QUINT, EXPO, CIRC, BOUNCE) dengan mapping ke Compose Easing; persistensi `animation_easing_curve` (default EASE_IN_OUT) & `animation_duration_ms` (default 300ms, range 100-1000ms step 50ms). (2) UI Settings Screen: Dialog/popup selector Animation Curve & VoxSlider durasi tipis 2dp dengan label dinamis real-time. (3) Global Motion Spec: Helper `getGlobalAnimationSpec` untuk tween/spring dinamis pada Sheet, Carousel snapping, Queue reorder, dan Floating Mini Player. (4) Kompilasi dan build APK `Vox_stable_v1.0.0.apk`. PENTING: DILARANG menjalankan perintah 'git add', 'git commit', atau 'git push'. (DONE)
   - **Instruction / ADB Install & Launch (Bluetooth Battery & Animation Easing)**: Build release APK `Vox_stable_v1.0.0.apk`, instal ke HP via ADB (`adb install -r Vox_stable_v1.0.0.apk`), dan langsung buka aplikasi dengan `am start -n com.vox.music/.MainActivity`. (DONE)
   - **Instruction / Git Stage, Commit & Push**: Melakukan staging seluruh file perubahan terbaru (Alphabet Fast Scroll, Dynamic Motion Blur, Queue Drag Bugfix & Edge Auto-Scroll, Bluetooth Battery Level Indicator, Animation Curve & Duration Customizer), membuat git commit, dan push ke repository GitHub `origin/main`. (DONE)
